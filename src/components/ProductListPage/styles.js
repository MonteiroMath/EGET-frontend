import styled from "styled-components";
import { StyledButton } from "../Shared/Buttons/StyledButton/styles";

export const BodyContainer = styled.main`
  padding: 65px 10px;
`;

export const NewProductButton = styled(StyledButton)`
  margin-left: 5vw;
`;
